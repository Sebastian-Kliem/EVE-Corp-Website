{% extends 'base.html.twig' %}

{% block title %}Login erfolgreich!{% endblock %}

{% block body %}
<div class="w-full max-w-[500px] mx-auto px-6 mt-16">
    <div class="bg-eve-card border border-eve-border backdrop-blur-md shadow-eve p-6 rounded-lg transition-all duration-300 hover:border-eve-primary hover:shadow-[0_10px_40px_rgba(0,240,255,0.15)]">
        <h1 class="text-2xl font-semibold text-center mb-6 text-eve-primary">✔️ Login erfolgreich!</h1>

        <div class="flex flex-col gap-4 mt-8">
            <a href="{{ path('home') }}" class="flex justify-between items-center w-full border border-eve-border hover:border-eve-primary text-eve-text hover:text-eve-primary bg-transparent rounded-lg px-5 py-4 transition-all duration-300 font-semibold no-underline cursor-pointer">
                <span>zurück zur Startseite</span>
                <span class="text-eve-primary">→</span>
            </a>
        </div>
    </div>
</div>
{% endblock %}
